<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.studentlifemanager">

    <application
        android:name=".ui.StudentLifeManager"
        android:icon="@drawable/ic_logo_gray"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo_gray"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudentLifeManager">
        <activity
            android:name=".ui.SplashActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.StudentLifeManager.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>